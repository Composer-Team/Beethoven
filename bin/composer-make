#!/bin/bash
if test $COMPOSER_ROOT -eq "" ;
then
	echo "\$COMPOSER_ROOT appears to be undefined!"
	exit
fi
mkdir -p verilator
cmake $COMPOSER_ROOT/Composer_Verilator -B verilator -DCMAKE_BUILD_TYPE=Debug
cd verilator && make
