// @ts-check

/**
 * Sidebars configuration for Beethoven documentation.
 * Since we use pages instead of docs, this file is kept minimal.
 * Navigation is handled through the navbar and footer in docusaurus.config.js.
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {};

export default sidebars;
